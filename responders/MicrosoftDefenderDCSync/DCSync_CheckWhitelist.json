{
    "name": "DCSync_CheckWhitelist",
    "version": "1.0",
    "author": "CySOC",
    "url": "https://github.com/TheHive-Project/Cortex-Analyzers",
    "license": "AGPL-V3",
    "description": "Evaluate a DCSync case against the user+host whitelist stored in Consul KV. Stable identifiers (Microsoft Defender device id, directory object id) are read from the MicrosoftDefender enrichment analyzer reports attached to the observables, so any spelling of the same user/host matches the same whitelist entry. All pairs whitelisted: closes the case as a false positive; otherwise tags it as a true positive. Fails without closing when an observable lacks enrichment.",
    "dataTypeList": [
        "thehive:case"
    ],
    "command": "MicrosoftDefenderDCSync/dcsync_whitelist.py",
    "baseConfig": "MicrosoftDefenderDCSync",
    "config": {
        "service": "check"
    },
    "configurationItems": [
        {
            "name": "thehive_url",
            "description": "TheHive base URL",
            "type": "string",
            "multi": false,
            "required": true,
            "defaultValue": ""
        },
        {
            "name": "thehive_api_key",
            "description": "TheHive API key with permission to read observables and close cases",
            "type": "string",
            "multi": false,
            "required": true,
            "defaultValue": ""
        },
        {
            "name": "consul_url",
            "description": "Consul HTTP API base URL",
            "type": "string",
            "multi": false,
            "required": true,
            "defaultValue": "http://consul.service.consul:8500"
        },
        {
            "name": "consul_kv_whitelist",
            "description": "Exact Consul KV path to the whitelist key (a single key holding a YAML document, not a directory prefix). Leave empty to skip the whitelist check — every pair is then treated as not whitelisted (true positive, case stays open)",
            "type": "string",
            "multi": false,
            "required": false,
            "defaultValue": ""
        },
        {
            "name": "consul_token",
            "description": "Consul ACL token (optional)",
            "type": "string",
            "multi": false,
            "required": false,
            "defaultValue": ""
        },
        {
            "name": "close_on_fp",
            "description": "Close the case automatically when every user+host pair is whitelisted",
            "type": "boolean",
            "multi": false,
            "required": false,
            "defaultValue": true
        }
    ]
}
